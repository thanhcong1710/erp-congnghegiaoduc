// Đây là một đoạn script mẫu (Code thực thi cố định)
// Agent có thể gọi script này để thực hiện các tác vụ lặp đi lặp lại mà không cần tự viết code lại từ đầu.

function processData(data) {
    console.log("Đang xử lý dữ liệu với cấu trúc cố định...");
    return data.map(item => item.toUpperCase());
}

module.exports = { processData };
